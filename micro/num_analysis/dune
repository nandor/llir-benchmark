;; Adapted from OCamlPro's ocamlbench-repo
;; See https://github.com/OCamlPro/ocamlbench-repo

(executable
  (name num_analysis)
  (libraries harness))

(alias (name build_micro) (deps num_analysis.exe))
