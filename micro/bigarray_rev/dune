;; Adapted from OCamlPro's ocamlbench-repo
;; See https://github.com/OCamlPro/ocamlbench-repo

(executable
  (name bigarray_rev)
  (libraries harness bigarray))

(alias (name build_micro) (deps bigarray_rev.exe))
