;; Adapted from OCamlPro's ocamlbench-repo
;; See https://github.com/OCamlPro/ocamlbench-repo

(executable
  (name vector_functor)
  (libraries harness))

(alias (name build_micro) (deps vector_functor.exe))
